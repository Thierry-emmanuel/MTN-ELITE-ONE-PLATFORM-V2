/**
 * Module bootstrap — the single place tenant modules are imported.
 * Adding module #20 (or #200) is one import line here; the shell
 * never changes.
 */
import '@/modules/admin-legacy/module';
